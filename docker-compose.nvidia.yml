services:
  dev:
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile.nvidia
      args:
        - USERNAME=${USER}
        - UID=${UID:-1000}
        - GID=${GID:-1000}
        - INPUT_GID=${INPUT_GID}
    image: creating_autonomous_car_ros2:jazzy-nvidia
    container_name: creating_autonomous_car_dev
    privileged: true
    network_mode: host
    runtime: nvidia
    environment:
      - DISPLAY=${DISPLAY}
      - XAUTHORITY=/tmp/.Xauthority
      - ROS_DOMAIN_ID=1
      - RMW_IMPLEMENTATION=rmw_cyclonedds_cpp
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=all
    volumes:
      - ${PWD}:/ws/src/creating_autonomous_car
      - ${PWD}/../cache/build:/ws/build
      - ${PWD}/../cache/install:/ws/install
      - ${PWD}/../cache/log:/ws/log
      - ${HOME}/.Xauthority:/tmp/.Xauthority
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /dev/input:/dev/input
    working_dir: /ws
    stdin_open: true
    tty: true
